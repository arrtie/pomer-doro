# @format

name: pomer-doro
services:
  astro:
    command: ["/bin/bash"]
    image: node:23-bookworm-slim
    ports:
      - 4321:4321
    tty: true
    volumes:
      - type: bind
        source: ../
        target: /opt/pomer-doro
